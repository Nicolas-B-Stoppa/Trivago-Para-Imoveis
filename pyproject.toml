[project]
name = "pesquisador-imoveis"
version = "0.1.0"
description = "Leitor de anuncios do Imovelweb"
requires-python = ">=3.11"
dependencies = [
    "beautifulsoup4>=4.12,<5",
    "selenium>=4.23,<5",
]

[project.optional-dependencies]
dev = ["pytest>=8,<9"]

[tool.pytest.ini_options]
testpaths = ["tests"]
